package roar;

import java.util.HashMap;
import java.util.Map;
import java.util.Scanner;
import java.util.TreeMap;

public class SDetect {
    public static void main(String[] args) {
/*        Map<Double, String> m = new HashMap<>();
        var f = new Scanner(System.in);
        while (true) {
            System.out.print("Enter Your Map Entry(Key: Value): ");
            var d = f.nextDouble();
            if (d <= 0.00) {
                break;
            }
            var c = f.next();
            m.put(d, c);


        }
        int i = 0;
        for (int j = 0; j < m.size(); j++) {
            for (int k = 0; k < m.size(); k++) {

            }
        }*
        for (Map.Entry<Double, String> s : m.entrySet()) {
            Double[] keys = m.keySet().toArray(new Double[0]);
            System.out.println(++i + ". " + "[" + s.getKey().intValue() + "->" + s.getValue() + "]");
            for

        }
    }*/
}}